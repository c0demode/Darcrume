<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.walderman.darcrume">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Controller_LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Controller_MainActivity"></activity>

        <activity android:name=".Controller_ManageFilmsActivity"></activity>

        <activity android:name=".Controller_ManageChemsActivity"></activity>

        <activity android:name=".Controller_Notes"></activity>

        <activity android:name=".Controller_DevelopActivity"></activity>

    </application>

</manifest>