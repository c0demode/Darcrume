<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.walderman.darcrume">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DevelopTimer"></activity>
        <activity android:name=".HowTo" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ManageFilmsActivity" />
        <activity android:name=".ManageChemsActivity" />
        <activity android:name=".Notes" />
        <activity android:name=".DevelopActivity" />
        <activity
            android:name=".EditAddFilm"
            android:parentActivityName=".ManageFilmsActivity" />

    </application>

</manifest>